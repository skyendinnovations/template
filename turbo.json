{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": { "cache": false },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "lint": {},
    "clean": { "cache": false },
    "db:generate": {},
    "db:migrate": {},
    "db:studio": { "cache": false }
  }
}
